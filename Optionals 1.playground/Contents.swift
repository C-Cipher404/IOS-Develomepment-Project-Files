import UIKit

import Foundation

var stationTemperature: Double?
// nil = sensor is offline or hasn't reported.

var testScore: Int?
// nil = student hasn't taken the test yet.

var phoneNumber: String?
// nil = no phone number on file.

var middleName: String?
// nil = this person has no middle name at all.

var discountPercentage = 0.0
// every product has a discount.

var manager: String?

// nil = this employee has no manager (CEO, top of the org chart).

var publicationDate = Date()
// a book that exists in the catalog has been published.

var arrivalTime: Date?
// nil = flight hasn't landed and has no estimate yet.

var expirationDate = Date()
// every item has expiration date.

var previousAddress: String?
// nil = this is their first address.

